name: Run Connector E2E Tests (staging)

on:
  pull_request:
    branches: [main]
    paths:
      - registry/**
    

jobs:
  setup-e2e:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.e2e-test-matrix.outputs.matrix }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          fetch_depth: 1

      - name: Get all connector version package changes
        id: connector-version-changed-files
        uses: tj-actions/changed-files@v44
        with:
          json: true
          escape_json: false
          files: |
            registry/**

      - name: Print out all the changed filse
        env:
          ADDED_FILES: ${{ steps.connector-version-changed-files.outputs.added_files }}
          MODIFIED_FILES: ${{ steps.connector-version-changed-files.outputs.modified_files }}
          DELETED_FILES: ${{ steps.connector-version-changed-files.outputs.deleted_files }}
        run: |
          echo "{\"added_files\": $ADDED_FILES, \"modified_files\": $MODIFIED_FILES, \"deleted_files\": $DELETED_FILES}" > changed_files.json

      - name: List changed files
        id: list_files
        run: |
          cat changed_files.json

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: 1.21.x

      - name: Get the list of e2e tests to run
        id: e2e-test-matrix
        env:
          CHANGED_FILES_PATH: "changed_files.json"
        run: |
          mv changed_files.json registry-automation/changed_files.json
          cd registry-automation
          MATRIX_JSON=$(go run main.go e2e)
          echo "$MATRIX_JSON"
          echo "matrix=$MATRIX_JSON" >> "$GITHUB_OUTPUT"

  e2e-tests:
    needs: setup-e2e
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        tasks: ${{ fromJSON(needs.setup-e2e.outputs.matrix) }}
    env:
      SELECTOR_PATTERN: ${{ matrix.tasks.selector_pattern }}
      CONNECTOR_VERSION: ${{ matrix.tasks.connector_version }}
      CLI_TAG: latest-staging
    steps:
      - name: test
        run: |
          echo "Running e2e tests for ${{ needs.setup-e2e.outputs.matrix}}"
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          fetch_depth: 1
      - name: Install bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Run e2e tests
        working-directory: ./registry-automation/e2e-testing
        env:
          HASURA_DDN_PAT: WSM4cGM1ChBIIykOHWRVv3rRsXwN7EdfK83g8xHE36MCUaXwmTRZccQYcMKx9pWd
        run: |
          bun install
          bun run start-ndc
